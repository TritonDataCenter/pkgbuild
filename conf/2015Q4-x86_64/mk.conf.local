#PYTHON_VERSION_DEFAULT=27
MYSQL_VERSION_DEFAULT=56
PGSQL_VERSION_DEFAULT=95
PKG_APACHE_DEFAULT=apache24

PKG_OPTIONS.node = openssl

PKG_OPTIONS.openssl = zlib threads

PKG_OPTIONS.nginx = dav status realip uwsgi memcache naxsi perl gzip headers-more
